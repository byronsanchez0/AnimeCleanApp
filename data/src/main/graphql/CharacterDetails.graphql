query GetCharacterDetails ($characterId: Int)
{
    Character(id: $characterId) {
        id
        name {
            full
        }
        age
        image {
            medium
        }
        description

    }
}